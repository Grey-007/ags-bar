This project must run on AGS v3 only.

Rules:
- Never import from "ags"
- Use ags/app, ags/widget, ags/variable, ags/process
- GTK4 only
- Windows self-register when imported
- app.js only starts runtime
- No deprecated API (App.config, Widget.*, Utils.exec)
Target: Hyprland

